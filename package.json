{
  "name": "sanctuary-explorer-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 3001",
    "check": "node --check app.js && node --check sanctuary-scene.js && node scripts/validate-accessibility.mjs"
  }
}
